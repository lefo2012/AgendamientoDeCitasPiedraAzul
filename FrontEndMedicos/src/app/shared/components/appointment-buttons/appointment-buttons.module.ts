import { NgModule } from '@angular/core';
import { AppointmentButtons } from './appointment-buttons';

@NgModule({
  imports: [AppointmentButtons],
  exports: [AppointmentButtons]
})
export class AppointmentButtonsModule {}